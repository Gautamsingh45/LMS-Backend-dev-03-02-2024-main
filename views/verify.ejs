<!-- views/verify.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verification</title>
    <style>
        /* Resetting default margin and padding */
body {
    margin: 0;
    padding: 0;
  }
  
  /* Flexbox to center the form vertically and horizontally */
  body {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background-color: #f4f4f4;
  }
  
  /* Container for the form */
  .container {
    background-color: #d8d8f8; /* Soft violet blue */
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    padding: 20px;
  }
  
  /* Heading styles */
  h2 {
  
    color: #333; /* Dark grey */
  }
  
  /* Form styles */
  form {
    text-align: center;
  }
  
  /* Label styles */
  label {
    font-weight: bold;
  }
  
  /* Input field styles */
  input[type="text"] {
    width: 100%;
    padding: 10px;
    margin-bottom: 20px;
    border: 1px solid #ccc;
    border-radius: 4px;
    box-sizing: border-box; /* Ensure padding is included in width */
  }
  
  /* Button styles */
  button[type="submit"] {
    background-color: #6a5acd; /* Medium slate blue */
    color: #fff; /* White text */
    padding: 10px 20px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }
  
  button[type="submit"]:hover {
    background-color: #483d8b; /* Dark slate blue on hover */
  }

  .top-logo {
    position: fixed;
    top: 0;
    left: 0;
    margin: 10px; 
  }
  
  .large-logo {
    max-width: 100%;
    max-height: 100px;
    height: auto;
  }

  
    </style>
</head>
<body>
    <div class="top-logo">
        <img class="large-logo" src="/images/redlogo.png" alt="Saarthi Logo">
      </div>
    <!-- <h2>Verify Email</h2> -->
    <form action="/verify" method="POST">
        <input type="hidden" name="email" value="<%= email %>">
        <label for="otp">Enter OTP:</label><br>
        <input type="text" id="otp" name="otp" required><br>
        <button type="submit">Verify Email</button>
    </form>
</body>
</html>
